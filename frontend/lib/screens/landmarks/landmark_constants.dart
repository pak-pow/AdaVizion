import 'package:flutter/material.dart';

// ─── BRANDING COLORS ──────────────────────────────────────────────────────────

/// Primary brand color used across all landmark UI.
const kLandmarkMaroon = Color(0xFF7A1D1D);

/// Darker maroon used in hero gradients.
const kLandmarkMaroonDark = Color(0xFF5D1414);

/// Fun-fact card palette.
const kFunFactGreen = Color(0xFF2E7D52);
const kFunFactMint = Color(0xFFF0FAF4);
const kFunFactSage = Color(0xFFB2DFC4);
const kFunFactDeep = Color(0xFF1A3D2B);
