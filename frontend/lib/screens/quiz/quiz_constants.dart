import 'package:flutter/material.dart';

// ─── BRANDING COLORS ──────────────────────────────────────────────────────────

const kQuizMaroon = Color(0xFF7A1D1D);
const kQuizMaroonDark = Color(0xFF4A0F0F);
const kQuizTakeColor = Color(0xFF700000);
const kQuizHeaderGrey = Color(0xFFF5F5F5);
const kQuizDivider = Color(0xFFDDDDDD);

/// Gradient used on the QuizListScreen header banner.
const kQuizGradientTop = Color(0xFFB11C1C);
const kQuizGradientBottom = Color(0xFF4B0C0C);

/// Pass / correct answer indicator.
const kQuizPassGreen = Color(0xFF2E7D32);

/// Fail / wrong answer indicator.
const kQuizFailRed = Color(0xFFC62828);
